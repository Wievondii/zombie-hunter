# Dev-1 工作日志 — 角色选择页面修复

## 模块：M1 角色选择页面修复
## 文件范围：
- `src/flow/screens.js`（修改 drawCharSelect 函数）

## 规范：
- 改为 2 行×3 列网格布局
- cardW=140, cardH=100, gapX=12, gapY=10
- 居中计算：总宽 = 3*140 + 2*12 = 444，起始 X = (640-444)/2 = 98
- 点击检测需要同步更新
- 保持现有角色卡片样式（颜色、名称、描述）

## 变更记录

### 2026-05-16 — 角色选择布局修复

**问题**：角色选择页面6个角色单行水平排列，总宽度900px超出屏幕640px，右侧角色不可见。

**修复内容**：
1. 将单行排列改为2行×3列网格布局
   - cols=3, rows=2, cardW=140, cardH=100, gapX=12, gapY=10
   - 总宽=444, 起始X=98; 总高=210, 起始Y=75
   - 使用 `col = i % 3, row = Math.floor(i / 3)` 计算位置
2. 更新点击检测逻辑，2行网格布局的点击区域正确映射
3. 保持现有角色卡片样式（颜色、名称、描述、hover效果）
4. 卡片内容紧凑排列（图标24×24 → 名称 → HP+速度 → 伤害 → 描述）

**变更文件**：
- `src/flow/screens.js` — `drawCharSelect` 函数，布局从单行改为2×3网格

## 验收自查
- [x] 6个角色全部在屏幕内可见（总宽444 < 屏幕640）
- [x] 鼠标悬停和点击正确对应每个角色卡片（通过 `col/row` 计算位置）
- [x] 构建通过（`npm run build`：42 modules transformed, 120.23 kB）
- [x] 返回的 layout 对象与 main.js 兼容（chars, hovereds, backHovered 字段一致）

## 备注
- 实际文件为 `src/flow/screens.js`，dev log 原写的 `src/ui/menus.js` 已修正
- `cardH` 从 190 改为 100 以适应 2 行布局（总高 210 < 屏幕高 360）
- 卡片内容紧凑化：图标从 40×40 改为 24×24，HP/速度合并一行
